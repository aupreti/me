machine:
  ruby:
    version: 2.4.3

dependencies:
  pre:
    - ./bin/setup

test:
  post:
    - bundle exec jekyll build
    - bundle exec htmlproofer ./_site --only-4xx --allow-hash-href --assume-extension --check-opengraph
- USER_NAME: aupreti
- USER_EMAIL: upreti.angela@gmail.com
